import TextEditor from "../components/Editor/TextEditor";

const Inbox = (props) => {
    return (
        <div>
            <h1>Inbox page</h1>
            <TextEditor />
        </div>
    );
}

export default Inbox;